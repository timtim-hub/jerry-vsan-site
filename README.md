# Jerry Vsan Motion Site

Next.js (App Router) + TypeScript + Tailwind CSS + Framer Motion.

## Local

```bash
npm install
npm run dev
```

## Deploy

Production deploy via Netlify CLI:

```bash
npx netlify deploy --build --prod
```

